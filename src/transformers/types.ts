export enum FrameField {
  Labels = 'labels',
  Line = 'Line',
  Time = 'Time',
  // Grafana Logs panel recognises 'detected_level' for automatic level coloring.
  DetectedLevel = 'detected_level',
}
